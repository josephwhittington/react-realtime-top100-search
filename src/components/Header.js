import React from 'react'

const Header = props => {
    return (
        <header>
            <h1>React Search</h1>
            <h3>This a a live filter app for the Billboard Hot 100</h3>
        </header>
    )
}

export default Header